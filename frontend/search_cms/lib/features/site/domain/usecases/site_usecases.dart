import 'site_insert_usecase.dart';

/*
  The collection of all use cases for the site feature.

  @param siteInsertUsecase The constructed site insert use case passed in
    to the constructor for this class
 */
class SiteUsecases {
  final SiteInsertUsecase siteInsertUsecase;

  SiteUsecases({
    required this.siteInsertUsecase,
  });
}
